# Netlify picks this file from the repo root (this folder).

[build]
  command = "npm run build"
  publish = "dist"

# index.html redirects to dashboard; keep SPA-style fallback for direct /dashboard.html visits.
[[redirects]]
  from = "/dashboard"
  to = "/dashboard.html"
  status = 301
